# Tests Directory - Public on GitHub
# Password protection removed for public repository

# Security headers
<IfModule mod_headers.c>
    Header set X-Robots-Tag "noindex, nofollow"
</IfModule>

# Disable directory listing
Options -Indexes
